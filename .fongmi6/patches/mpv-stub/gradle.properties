# 独立构建的 stub 工程需要显式启用 AndroidX：
# AGP 的 AndroidXDependencyCheck 检测到 compileOnly media3-common/androidx.annotation
# 时，若未启用该属性，extractReleaseAnnotations 会在配置阶段直接报错。
android.useAndroidX=true

# 给 AGP/注解提取任务留足堆内存（CI 上 --no-daemon 单次构建）
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=512m
